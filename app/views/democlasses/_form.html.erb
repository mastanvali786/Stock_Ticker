<%= form_for(@democlass) do |f| %>
  <% if @democlass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@democlass.errors.count, "error") %> prohibited this democlass from being saved:</h2>

      <ul>
      <% @democlass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
